{
  "name": "ia-control-face-indexer",
  "version": "1.0.0",
  "description": "Lambda para indexar rostros de empleados en Rekognition",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "deploy": "zip -r function.zip index.mjs package.json && aws lambda update-function-code --function-name ia-control-face-indexer --zip-file fileb://function.zip --region us-east-1"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0"
  }
}
